# CLO-81: Add brand, tags, features fields to Products collection

- Added `brand` relationship field (→ brands)
- Added `tags` text field (comma-separated)
- Added `features` array field (product badges)
- Updated PDP to render all new fields dynamically
