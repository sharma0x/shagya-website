# CLO-32: Database seed script

## Overview

Seed script for dev data: admin user, sample categories, 20 saree products, sample pages, global settings. `pnpm seed` command.

## Acceptance Criteria

- [ ] `scripts/seed.ts` using Payload Local API
- [ ] Creates admin user
- [ ] Creates sample categories (silk, cotton, bridal, etc.)
- [ ] Creates 20 sample saree products with variants
- [ ] Creates sample pages (home, about, contact, faq)
- [ ] Updates global site settings
- [ ] `pnpm seed` runs from package.json scripts
