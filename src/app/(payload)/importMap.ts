/* ==========================================================================
   Payload Import Map — used by admin panel for custom components
   Add entries for custom admin components in Phase 2+.
   ========================================================================== */

import type { ImportMap } from 'payload'

export const importMap: ImportMap = {
  // Custom admin components go here (Phase 2)
  // Example:
  // '@/components/admin/CustomDashboard': () => import('@/components/admin/CustomDashboard'),
}

export default importMap
